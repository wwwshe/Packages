## Preview

이 모듈은 **개발용 프리뷰** (Xcode / SwiftUI Preview) 만을 위한 코드 모듈입니다.

- `UIViewPreview`, `UIViewControllerPreview` 를 통해 UIKit 뷰/뷰컨을 SwiftUI Preview 로 띄우는 용도

> 규칙  
> - 실제 앱 런타임에 필요한 코드는 다른 모듈에 두고,  
> - **프리뷰 전용 코드/헬퍼만** 이 모듈에 둡니다.

